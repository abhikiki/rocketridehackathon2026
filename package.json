{
  "name": "rocketride-error-pipeline-track-b",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsx check.ts",
    "setup": "npm install --prefix victim-app && npm install --prefix dashboard && npm install --prefix track-b-relay",
    "test:all": "npm run check && npm run typecheck && npm test --prefix victim-app && npm test --prefix track-b-relay && npm run build --prefix dashboard",
    "typecheck": "tsc --noEmit",
    "validate:rocketride": "tsx check.ts --remote"
  },
  "dependencies": {
    "express": "^4.22.2",
    "rocketride": "^1.3.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}

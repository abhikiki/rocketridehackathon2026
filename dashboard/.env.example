# Supabase project URL + anon (public, read-only via RLS) key — from
# Supabase dashboard > Project Settings > API. NOT the pooler connection
# string (that's server-side only, used by victim-app).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
